apiVersion: apps/v1
kind: Deployment
metadata:
  name: campvista-deployment
  namespace: campvista
  labels:
    app: campvista
spec:
  replicas: 2
  selector:
    matchLabels:
      app: campvista
  template:
    metadata:
      labels:
        app: campvista
    spec:
      containers:
        - name: campvista
          image: rohitar/campvista:CV3
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          resources:
            limits:
              cpu: "500m"
              memory: "512Mi"
            requests:
              cpu: "250m"
              memory: "256Mi"
